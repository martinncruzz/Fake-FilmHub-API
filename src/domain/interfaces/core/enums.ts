export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum ResourceType {
  USERS = 'users',
  MOVIES = 'movies',
  GENRES = 'genres',
  REVIEWS = 'reviews',
}

export enum OAuthProvider {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}
